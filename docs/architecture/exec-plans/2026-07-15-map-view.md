# Exec Plan — Map view for the home screen (Mapbox)

- **Date:** 2026-07-15
- **Author:** Claude + achrafbenamrane
- **Intent:** Client request (Farid demo) — see `docs/reports/daily/2026-07-14.md`
- **Status:** shipped — map verified on a physical Android device (bubbles, filter, brand
  styling). Route drawing not yet confirmed on device.

## Goal & non-goals

The Accueil tab gains a **list ⇄ map switch**. In map mode, every available deal is
a brand-red price bubble at its merchant's real Toulouse location; the category
filter still applies and filters the bubbles; selecting a bubble draws a **road
route** from the user to that merchant and shows distance + ETA. Done means: on a
device, switching to map shows bubbles, tapping a filter removes non-matching
bubbles, and tapping a bubble draws a red route from the blue dot.

**Non-goals.** No merchant-side map, no live delivery tracking, no turn-by-turn
navigation, no custom Mapbox Studio basemap (standard style + brand overlays —
the Studio style URL is a later one-line swap). No backend: merchant coordinates
stay static in `catalog.ts`, like the rest of the demo catalog.

## Already exists — reuse, do not recreate

- `packs/maps-view/src/nearby.ts` — **haversine `distanceKm(a, b)`**. Copy this; do
  NOT copy `fetchNearby` (it queries a Supabase table — we have no backend).
- `packs/maps-view/src/model/marker.ts` — `MapMarker` Zod shape to model bubbles on.
- `packs/maps-view/src/ui/map-screen.tsx` — **do not use**: it is a `react-native-maps`
  placeholder. We need Mapbox for the brand styling.
- `src/features/shop/model/catalog.ts` — merchants/deals live here; add `coord` here.
- `src/features/shop/ui/components/category-bar.tsx` — the filter is already built and
  already lifts state to `HomeScreen`. Map mode reuses it as-is; do not fork it.
- `src/features/shop/ui/home-screen.tsx` — owns `category` + `query` state; the
  switch and the map mount here so both views share one filter state.
- `src/shared/theme/colors.ts` — brand palette for bubbles/route. No hardcoded hex.

## Tasks

- [x] T1 — add `coord {lat,lng}` to `Merchant` + real Toulouse coordinates for every
      merchant → `src/features/shop/model/schema.ts`, `model/catalog.ts` (AC-1)
- [x] T2 [P] — port `distanceKm` from the pack → `src/features/shop/lib/geo.ts` + test (AC-4)
- [x] T3 — install `@rnmapbox/maps` + `expo-location`; register plugins; read the
      public token in `app.config.ts` → `extra` → `app.config.ts` (AC-1)
- [x] T4 — depends: T3 — new **dev build** on EAS (native module ⇒ no hot reload) (AC-1)
- [x] T5 — list⇄map switch → `src/features/shop/ui/components/view-switch.tsx` (AC-2)
- [x] T6 — depends: T1,T3 — map screen w/ brand price bubbles, honouring `category`
      → `src/features/shop/ui/map-view.tsx` (AC-1, AC-3)
- [x] T7 — depends: T6 — user location (permission + blue dot) (AC-4)
- [x] T8 — depends: T7 — Directions API route + distance/ETA card
      → `src/features/shop/lib/directions.ts` + test (AC-4)

## Phases

### Phase 1 — Data + geometry (no token needed)

- [x] T1, T2
      **Deliverable / proof:** `distanceKm` unit test passes; every merchant has a coord.

### Phase 2 — Native plumbing

- [x] T3, T4
      **Deliverable / proof:** new dev build installs and boots with Mapbox linked.

### Phase 3 — Map UI

- [x] T5, T6, T7 — verified on device
      **Deliverable / proof:** device screenshot — bubbles filtered by category.

### Phase 4 — Routing

- [x] T8 (code + 8 unit tests; unverified on device until T4)
      **Deliverable / proof:** device screenshot — red route from blue dot to merchant.

## What actually bit us (kept for the next agent)

- **A horizontal `ScrollView` in a flex column stretches vertically.** The category
  filter silently grew to ~420px and squashed the map into the space below it. It only
  showed in map mode — in list mode the same component sits in a `FlatList` header,
  which doesn't stretch it. The symptom (white gap above a short map) looks exactly like
  a Mapbox sizing bug, and two fixes aimed at Mapbox missed because of it. Fix:
  `flexGrow: 0`. **Lesson: tint the container and look, before theorising about the child.**
- **Centring the camera on the user showed an empty map.** Every merchant is in Toulouse,
  so testing from outside it rendered zero bubbles while the map worked perfectly. The
  camera must open on the _offers_, not the viewer.

## Risks & landmines

- **Native module ⇒ no hot reload.** Nothing in Phases 3–4 is visible on the current
  dev client. A fresh dev build (T4) gates all UI feedback. Budget ~10 min per build.
- **Two different Mapbox tokens.** Public `pk.*` renders the map and is _meant_ to ship;
  secret `sk.*` (DOWNLOADS:READ) is build-time only and must live as an EAS secret.
  Never commit either.
- **`check-secrets` false positive.** `scripts/check-secrets.mjs` flags any
  `EXPO_PUBLIC_*` name containing `TOKEN` — so `EXPO_PUBLIC_MAPBOX_TOKEN` would fail
  `verify` and block the pre-commit hook. Read the token in `app.config.ts` at build
  time and pass it via `extra` instead; do not weaken the rule.
- **Directions API is a network call** on a catalog that is otherwise fully offline.
  It must fail soft: no route ⇒ still show the bubble and the straight-line distance.
- **Disk at 98%.** Local bundling has already died once with `no space left on device`.
  EAS builds are unaffected (cloud), local Metro is not.
- **`packs/` is ESLint/tsconfig-excluded** — importing from it in app code is banned by
  the boundaries rule. Port the helper into `src/`, don't import across.

## Verification

- [ ] `npm run verify` green (note: `typecheck`/`lint` already fail on
      `supabase/functions/*` Deno globals — pre-existing, not caused here)
- [ ] CUJ: browse deals — exercised in both list and map mode on a device
- [ ] Docs updated: `docs/quality/critical-user-journeys.md` (map mode of the browse CUJ)
- [ ] Feedback encoded: geo helper has a unit test; token handling documented in
      `docs/conventions/environments.md`
